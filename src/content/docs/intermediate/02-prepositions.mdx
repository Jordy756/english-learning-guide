---
title: Preposiciones (In, On, At)
description: Aprende a usar las preposiciones de tiempo y lugar con lógica de programación.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import { Card, CardGrid } from '@astrojs/starlight/components';

Las preposiciones `in`, `on` y `at` se utilizan para indicar **tiempo** y **lugar**. La regla de oro es: **"In"** es para lo más general o grande, **"On"** es para superficies o puntos intermedios, y **"At"** es para lo más específico o pequeño. Una vez que entiendes esa lógica, elegir la preposición correcta se vuelve mucho más sencillo.

## 📋 Tabla de Preposiciones

```typescript
const Prepositions = {
  in: {
    tiempo: "General: siglos, años, meses, estaciones, partes del día",
    lugar: "Espacios cerrados o áreas grandes: Países, ciudades, dentro de algo"
  },
  on: {
    tiempo: "Más específico: Días de la semana, fechas exactas, días especiales",
    lugar: "Superficies o medios de transporte: Calles, objetos sobre otros, transporte público"
  },
  at: {
    tiempo: "Muy específico: Horas precisas, momentos puntuales",
    lugar: "Puntos específicos: Direcciones exactas, sitios concretos de reunión"
  }
} as const;
```

---

## 🕐 Preposiciones de Tiempo

<Tabs>
  <TabItem label="In (General)">

```javascript
// IN: Para periodos generales de tiempo
const timeExamples = {
  siglos: "in the 21st century",
  años: "in 2024",
  meses: "in January",
  estaciones: "in summer",
  partesDia: "in the morning / in the afternoon"
};

console.log(`I was born ${timeExamples.años}`);           // "I was born in 2024"
console.log(`We travel ${timeExamples.estaciones}`);      // "We travel in summer"
console.log(`I exercise ${timeExamples.partesDia}`);      // "I exercise in the morning"
```

  </TabItem>

  <TabItem label="On (Específico)">

```javascript
// ON: Para días y fechas específicas
const timeExamples = {
  diasSemana: "on Monday",
  fechas: "on December 25th",
  diasEspeciales: "on my birthday"
};

console.log(`I work ${timeExamples.diasSemana}`);         // "I work on Monday"
console.log(`We celebrate ${timeExamples.fechas}`);       // "We celebrate on December 25th"
console.log(`I party ${timeExamples.diasEspeciales}`);    // "I party on my birthday"
```

  </TabItem>

  <TabItem label="At (Muy Específico)">

```javascript
// AT: Para horas y momentos puntuales
const timeExamples = {
  horas: "at 3:00 PM",
  momentos: "at noon / at midnight",
  noche: "at night"  // Excepción importante
};

console.log(`The meeting is ${timeExamples.horas}`);      // "The meeting is at 3:00 PM"
console.log(`I eat lunch ${timeExamples.momentos}`);      // "I eat lunch at noon"
console.log(`I study ${timeExamples.noche}`);             // "I study at night"
```

  </TabItem>
</Tabs>

:::caution[Excepción de Tiempo]
Decimos "In the morning", "In the afternoon", pero para la noche siempre es **"At night"**.
:::

---

## 📍 Preposiciones de Lugar

<Tabs>
  <TabItem label="In (Áreas Grandes)">

```javascript
// IN: Para espacios cerrados o áreas grandes
const placeExamples = {
  paises: "in Mexico",
  ciudades: "in New York",
  dentroDeAlgo: "in the car / in the room"
};

console.log(`I live ${placeExamples.paises}`);            // "I live in Mexico"
console.log(`She works ${placeExamples.ciudades}`);       // "She works in New York"
console.log(`The keys are ${placeExamples.dentroDeAlgo}`);// "The keys are in the car"
```

  </TabItem>

  <TabItem label="On (Superficies)">

```javascript
// ON: Para superficies y transporte grande
const placeExamples = {
  calles: "on Main Street",
  superficies: "on the table",
  transporte: "on the bus / on the plane"
};

console.log(`I live ${placeExamples.calles}`);            // "I live on Main Street"
console.log(`The book is ${placeExamples.superficies}`);  // "The book is on the table"
console.log(`She is ${placeExamples.transporte}`);        // "She is on the bus"
```

  </TabItem>

  <TabItem label="At (Puntos Específicos)">

```javascript
// AT: Para direcciones exactas y puntos de reunión
const placeExamples = {
  direcciones: "at 123 Main Street",
  lugares: "at the office / at school",
  puntosReunion: "at the bus stop"
};

console.log(`I am ${placeExamples.direcciones}`);         // "I am at 123 Main Street"
console.log(`She is ${placeExamples.lugares}`);           // "She is at the office"
console.log(`Meet me ${placeExamples.puntosReunion}`);    // "Meet me at the bus stop"
```

  </TabItem>
</Tabs>

---

## 🚗 Nota sobre Transporte


  <Card title="In (Vehículos Pequeños)" icon="approve-check">
    Usas **"In"** para vehículos pequeños donde no puedes caminar.
    ```javascript
    const smallVehicles = ["in a car", "in a taxi"];
    ```
  </Card>
  <Card title="On (Vehículos Grandes)" icon="rocket">
    Usas **"On"** para vehículos grandes donde puedes estar de pie o caminar.
    ```javascript
    const largeVehicles = ["on a plane", "on a train", "on a ship"];
    ```
  </Card>

